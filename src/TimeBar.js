import React, {PropTypes} from "react";

const TimeBar = React.createClass({
    getInitialState () {
        return {};
    },

    render () {
        return (
            <div>Time Bar</div>
        );
    }
});

export default TimeBar;
